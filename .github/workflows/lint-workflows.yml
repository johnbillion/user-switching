# yaml-language-server: $schema=https://json.schemastore.org/github-workflow

name: Lint workflow files
on:
  pull_request:
    paths:
      # Only run when changes are made to workflow files.
      - '.github/workflows/**'
  push:
    branches:
      - develop
      - trunk
    paths:
      # Only run when changes are made to workflow files.
      - '.github/workflows/**'

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    permissions:
      security-events: write # Required for upload-sarif to upload SARIF files.
      contents: read         # Needed to clone the repo.
      actions: read          # Needed for upload-sarif to read workflow run info.
    uses: johnbillion/plugin-infrastructure/.github/workflows/reusable-workflow-lint.yml@3e82a27abb232ebb50b7f13b52038d5d7a778df1
